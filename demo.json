{
	"server": {
		"name": "localhost",
		"port": 5777,
		"rules": {
			"allow": ["^https?://"],
			"deny": [
				"^https?://localhost",
				"^http://127.0.0.1"
			]
		},
		"debug": false
	},
	"client": {
		"redirect": "http://localhost:5777/",
		"rules": {
			"allow": [""],
			"deny": [
				"^https?://localhost",
				"^http://127.0.0.1"
			]
		}
	}
}